# KISP 업무 자동화 매니저
# Python 3.11+ 필요

# ── GUI ─────────────────────────────────────────────
PyQt6>=6.6.0
qtawesome>=1.3.0

# ── Excel ───────────────────────────────────────────
openpyxl>=3.1.0

# ── Word ────────────────────────────────────────────
python-docx>=1.1.0

# ── Windows 자동화 (Word→PDF, 바로가기 생성, 레지스트리)
# Windows 전용
pywin32>=306; sys_platform == "win32"

# ── 보안 ────────────────────────────────────────────
# 비밀번호를 Windows 자격 증명 관리자에 저장
keyring>=24.0.0

# ── MySQL ───────────────────────────────────────────
mysql-connector-python>=8.0.0
# 쿼리 관리 탭의 "SQL 포맷" 버튼 사용 시 필요
# pip install sqlparse
sqlparse>=0.5.0
